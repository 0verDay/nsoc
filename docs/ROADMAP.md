# NSOC 重构路线与进度

> 现行规范。完整方案见仓库根目录 `重构文档.md`（评审稿，含分层禁令、反作弊设计、清理清单与验收表）。
> 本文件只记录**实时状态**，每次切片完成后更新。最后更新：阶段 3 切片 3 完成。

## 一句话状态

**阶段 0 完成；阶段 1/2/3 进行中，整体约完成 60~65%。**
分支 `refactor`（基线 tag `pre-refactor`），17 个提交，全部以"六套 headless 测试全绿 + 两条黄金路径状态哈希逐位不变"作为行为不变的证据。

## 四阶段状态

| 阶段 | 进度 | 已完成 | 未完成 |
|---|---|---|---|
| **0 准备与安全网** | ✅ 100% | CI 三件套（分层棘轮 / 内容与注册表一致性 / 解析）；状态哈希 `StateHash`；两条黄金路径；6 套 headless 测试；`.gitignore` 补全 | — |
| **1 反作弊止血 + 权威骨架** | 🚧 ~50% | `NetProtocol`（v2 协议、服务器专属消息定义）；`BattleAuthority`（四道校验 + 私有视图 + 权威事件）；`BattleServerSession`（握手 / 伪造消息丢弃 + 审计 / 按玩家路由）；78 条断言 | **Go 侧止血**（`server/*.go` 仍是纯转发：type 白名单、`game/start` 房主校验、身份字段重写、seq·限速）；**客户端接入权威路径** |
| **2 结构归一** | 🚧 ~75% | 显式注册表（4 张表 + CI 一致性校验）；目录归位（`scripts/` 根目录清零）；章节脚本三合一（−427 行）；斩断 `core→ui`（违规 181→122）；三段 bootstrap 去重 + 死信号清理；显式 `BattleMode`；行动定序去 UI 像素；规则随机源集中；多棋盘黄金路径 | `BattleSession` 类提取；**`main.gd`(862) ⟷ `test_main.gd`(1389) 合一**（553 行逐字重复 + 18 节点场景克隆）；UI 三胞胎收敛（3 面板管理器 / 3 轮播 / 20 份滚动物理副本） |
| **3 分层深化 + 反作弊收口 + 清理** | 🚧 ~40% | 行动定序去像素；规则随机源集中；死代码清理（45 定义 + 1 死文件，−347 行）；战斗结算与表现分离；动画等待抽象 + 瞬时模式（违规 122→87）；构建产物出库 | **`cell.gd`(393) 数据/表现分离**（连锁约 35 处读写点，最大一块）；`board_slot_factory` / `play_controller` 的 `Control` 依赖；`turn_system` 剩余 `await` 语义梳理；反作弊收口；**文档归档**（本批正在做） |

## 对照四条目标要求

| 要求 | 进度 | 判定依据 |
|---|---|---|
| 1 结构健康、保留扩展性 | ~70% | 分层违规 **181→87**；目录与分层一一对应；新增扩展点（`BattleMode` / 显式注册表 / 协议层）都已落地并有 CI 守护 |
| 2 确保无法作弊 | ~50% | 服务器侧边界完整（逐条封堵审计中的灾难级漏洞）；**客户端未接入**，未形成端到端权威链路 |
| 3 双端更新流程简单 | ~80% | 规则/数据/协议各只有一份；版本 + 内容哈希握手已实现；三件产物打包与内容热更未落地 |
| 4 删冗余文档与无用脚本 | ~75% | 代码死代码清理 + 构建产物出库已完成；本批把根目录 6 份文档归档进 `docs/archive/`、删掉 `step8_content.txt`、建立 `docs/` 现行规范；`dev1/` 与 4 个已合并的远端旧分支待处理 |

## 剩余工作（按建议顺序）

1. **文档收尾**：`dev1/` 标注废弃、删除 4 个已合并的远端旧分支（需你确认）
2. **`cell.gd` 数据/表现分离**（最大一块；前置：本项与 3 号互相支撑）
3. **客户端接入权威路径**：把玩家操作产出为 `intent/*`、消费 `auth/*`；需要在权威端跑通棋盘结算
4. **Go 侧止血**：type 白名单 / `game/start` 房主校验 / 身份字段重写 / seq·限速（**需要 Go 构建环境**）
5. **`main`/`test_main` 合一**（风险最高，已有两条黄金路径兜底）
6. 次要：`BattleSession` 提取、UI 三胞胎收敛、`Control` 依赖清理

## 验收标准（每阶段结束逐条勾）

- [x] CI 通过：脚本解析 / 分层规则 / 内容与注册表一致性 / 一局状态哈希
- [ ] `core/` 内 `Control|Panel|Node2D|add_child|create_tween|create_timer|get_tree()|has_method` 计数 = 0（当前 87 处，全部为既有硬骨头）
- [x] `core/` → `ui/` 的 `class_name` 引用 = 0
- [x] 篡改服务器消息被拒：伪造断线 / 判胜 / 重开 / 结果字段全部丢弃且不影响他人（`ServerSessionTest`）
- [x] 抓包验证：对手手牌不可见（`auth/state` 只给数量）
- [x] 同种子 + 同输入 → 状态哈希一致（跨进程、跨副本、跨分辨率、跨"瞬时/普通"模式）
- [x] 新增一张卡 = 只改 `data/all_cards.json`（+ 新效果时登记一行）
- [x] 新增一关 = 只加一个 JSON
- [ ] 一条命令产出三件产物 + `version.json`
- [x] `git ls-files` 无 `*.exe` / `*.log` / `*.pyc`
- [x] 根目录只剩 `README.md` + `重构文档.md` + 工程目录（本批归档后）
- [x] 死代码清单全部清除且无回归
- [ ] 1v1 / 1v3 / 3v3 / 战役 / 演义 五条路径全流程冒烟通过（当前覆盖：战役 ✅ 单盘、多棋盘 PVE ✅；PVP 三条依赖客户端接入）
