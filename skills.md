# NSOC (Neo Soul of Card) 开发文档与规则总结

本备忘录和技能文档旨在为参与 `nsoc` 项目开发的 AI 和开发者提供当前的开发进度、架构和游戏核心规则的总结。

**重要原则：今后的所有相关修改和后续迭代，均只在 `dev_gd` (Godot) 版本中进行。`dev1` (Web 前端测试版) 已停止维护。**

## 1. 项目基本架构
- **根目录**: `c:\Users\yuyi08\Documents\GitHub\nsoc`
- **主要开发目录**: `dev_gd/nsoc/` - 基于 Godot 引擎研发。
- **配置数据路径**: `dev_gd/nsoc/data/` 目录用于存放 JSON 配置文件（需确保导出或运行 Godot 时包含此格式）。

## 2. 核心数据与配置读取
- 游戏核心数值采用外部 JSON 文件配置，读取时强制转为 `int` 以保证逻辑与计算的正确性。
- **英雄/玩家数据** (`data/test_hero.json`): 包含双方基准血量（目前双发默认血量为 30）。
- **卡牌/单位数据** (`data/test_card.json`): 包含当前可用卡牌池，目前支持卡牌具有以下属性：
  - `name`: 卡牌名称（例: "blanket", "pro"）
  - `type`: 卡牌类型（例: "单位"）
  - `cost`: 施放花费费用
  - `health`: 基础生命值
  - `attack`: 基础攻击力

## 3. 游戏规则与战斗机制 (Auto-Battler + Card)

### 3.1 地图与坐标
- 地图基础尺寸为 6行 x 3列。
  - 行数索引：0 - 5 (共 6 行)
  - 列数索引：0 - 2 (共 3 列)
- 玩家可用放置区域通常为底平面的自家部分，游戏代码中逻辑按行号和列号计算：
  - 敌方阵地趋向于上方（行 0 靠近敌方英雄）。
  - 玩家阵地趋向于下方（行 5 靠近玩家英雄）。

### 3.2 玩家费用与资源
- **最大费用 (Max Mana)**: 10 费。
- 每回合会自动补满 10 费用（测试期机制）。
- **手牌数量**: 使用卡牌后，会自动补牌以确保玩家手中持有最少数量要求的手牌（目前为 5 张），补牌逻辑从读取好的卡牌池里随机抽取。

### 3.3 回合流程与单位行为 (自走棋逻辑)
玩家在己方回合消耗费用将手牌上的单位放置到网格的空位格子上，完成布置后点击“结束回合”，触发自动行动阶段。

- **遍历顺序**:
  - **玩家棋子**: 按行 0 到 5，列 0 到 2 的顺序依次寻找己方单位触发行动。
  - **敌方棋子**: 按行 5 到 0，列 2 到 0 的反向顺序依次寻找敌方单位触发行动。
- **行动优先级**:
  1. **索敌与攻击**: 
     - 首先寻找四邻接格（上下左右）是否存在敌对单位。
     - 若存在，优先发动漫长进行互击计算（互相扣除各自的攻击力数值，若血量 `≤0` 则单位死亡，从格子清理）。
  2. **攻击英雄**: 
     - 若单位位于最前线（玩家单位在 `row == 0`，敌方单位在 `row == 5`）并且无法攻击敌方棋子，则直接对敌方首领（Player Health / Enemy Health）造成等同于该单位攻击力的直接伤害。
  3. **移动 (向前推进)**: 
     - 如果周边没有敌人，且未处于端线，则棋子向敌方底线前进一步（玩家单位：`row - 1`；敌方单位：`row + 1`），前提是目标格子为空。
- 每个单位在一回合中只能攻击/移动执行一次行动（由 `has_attacked` 状态控制）。

## 4. UI与表现逻辑
- 网格格子 (Cell) 与手牌卡片 (HandCard) 均为预制好的 Godot Scene (`Cell.tscn`, `HandCard.tscn`)。
- 主要游戏管理和 UI 更新均在 `scripts/main.gd`。
- **视觉风格**:
  - UI 样式基于 `StyleBoxFlat` 在 GDScript 中以代码动态生成，用于控制卡牌背景颜色、圆角、边界以及阴影。
  - 敌方视觉效果主要偏红色调，玩家为中性/明亮色系。

## 5. 今后开发需遵守的规范
1. **只动 `dev_gd`**: 所有修改严格控制在 `dev_gd/nsoc/` 及其子目录内。
2. **纯粹的类型处理**: 处理 JSON/外部引入数据中的可计算数值时，须保证显式的数据类型转换（例如 `int(val)`）。
3. **保持自走战棋逻辑的独立**: 在补充新的词条（例如：剧毒、溅射、多重攻击、冲锋等）或行为规则时，需谨慎融入当前的 `run_turn_sequence` 顺序执行循环中。