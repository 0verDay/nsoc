# NSOC (Godot 卡牌战棋项目) 架构与技术总结

## 1. 项目概述
本项目是一个基于 Godot 4.x (GDScript) 开发的卡牌战棋类游戏。游戏核心玩法融合了手牌管理机制和 3x3 网格战棋单位的布局对弈。通过数据驱动 (如 JSON 配置) 以及动态资源加载构建了高度可扩展的前端与对战框架。

## 2. 核心系统架构

### 2.1 卡牌生命周期流转 (Deck Lifecycle System)
项目内建了极为严谨的卡片流转闭环系统，分为三大核心存储区：
- **牌库区 (Draw Pile)**: 初始化时读取配置进入此区域；抽卡时从尾部 `pop_back()` 取出交于玩家手牌区。
- **墓地区 (Graveyard)**: 默认卡牌/单位死亡时的最终归宿。当牌库被抽空时，系统会自动触发切洗逻辑，将墓地内的所有卡牌转移回牌堆并应用 `shuffle()` 打乱。
- **除外区 (Banished)**: 为受到特定效果支配而彻底脱离本局循环池的卡牌所设计的隔离储存区。

### 2.2 效果脚本热插拔机制 (Modular Effects System)
为了实现不同卡牌机制和属性的可复用与解耦，专门抽象构建了独立的 `effects` 脚本目录：
- 使用卡牌配置中的字符串数组 (如 `"effects": ["ash"]`) 去映射并加载具体的 GDScript。
- 在关键触发点 (例：`_handle_card_death`) 进行轮询探查探查其是否存在事件方法。如果存在且返回了处理被接管的标志 (返回 `true`)，系统则会将实体的数据全权交由独立效果脚本结算处理 (如 `ash.gd` 使死亡后直接将数据压入除外区而不是墓地)。

### 2.3 网格渲染与物理战场 (Grid & Board)
- 战场分为上下两个 3x3 专属网格 (对应己方与敌方位置)，使用 `GridContainer` 确保每个格子的自适应安全居中。
- 点击与放置逻辑：采用 Godot 的 `gui_input` 与 `mouse_enter` 等 UI 接管替代传统物理射线，实现了更加精准长按响应及拖拽放置 (`_can_drop_data` 与 `_drop_data` 工作流)。

### 2.4 可复用参数化界面 (Dynamic UI & Styling)
- **按编程式视觉主题渲染**: 大幅削减了静态贴图资源的使用，利用代码内聚的 `StyleBoxFlat` 构建器实现全局圆角、颜色渐变和阴影按钮的复写应用，确保全局视觉高度一致。
- **响应式排版**: 如根据容器动态排列侧边栏、计算锚点位置 (`Anchor Preset`)、自适应均分布局 (`size_flags_vertical`) 和微调 `separation` 间距确保卡牌视觉的整洁。