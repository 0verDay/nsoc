# NSOC 底层重构文档

> 状态：**待评审**（评审通过后开工）
> 适用范围：`dev_gd/nsoc/`（Godot 客户端 + 服务端）、`server/`（Go 网关）、仓库根目录
> 依据：三份只读审计（信任边界 / 规则可移植性 / 场景装配层 / 代码健康度）+ 实测数据
> 原则：**一次计划、分阶段落地、每个阶段可独立发布、任何阶段都不破坏已有功能**

---

## 0. 决策摘要（本方案的前提）

| 议题 | 决定 |
|---|---|
| 反作弊目标 | **服务器权威**：服务器是唯一规则执行者，客户端只发"意图"、收"权威结果" |
| 实现载体 | **Godot 无头服，复用同一套 GDScript**（不把规则翻写成 Go） |
| PVP 形态 | **不保留无服务器的锁步 P2P**，PVP 一律走服务器 |
| 演义（帝国）模式 | **纳入接口改造**（硬编码关卡数据外提 + 判定点改回调） |
| 文档 | 现行规范重写进 `docs/`，历史设计稿移入 `docs/archive/` |
| 平台 | 客户端 **Android**（另保留 Windows 客户端用于开发调试）；服务端 **Windows** |
| 节奏 | 允许一次 **2~4 周冻结窗口**，之后转增量 |

---

## 1. 四条目标与可验收标准

| # | 目标 | 验收标准（可机器检查） |
|---|---|---|
| 1 | 项目结构健康、保留扩展性 | ① `core/` 内零 `Control/Panel/Node2D/add_child/create_tween/get_tree()`；② `core/` 零 `has_method`；③ 新增一张卡只动 `data/` + 至多一个 `effects/*.gd`；④ 新增一个关卡/剧本只动 `data/`；⑤ CI 上分层规则可自动校验并失败 |
| 2 | 确保无法作弊 | ① 客户端上报的"结果字段"从协议中消失；② `disconnect/notify`、`game/end`、`game/start` 客户端发包被服务器拒绝；③ 洗牌种子与手牌只存在于服务器；④ 任一客户端抓包看不到对手手牌；⑤ 改客户端无法改变任何权威状态（用"篡改客户端"冒烟测试验证） |
| 3 | 更新服务端和客户端流程简单 | ① 一条命令产出三件产物（Android 客户端 / Windows 客户端 / Windows 无头服）；② 规则与内容只有一份，改玩法不需要改两遍；③ 协议版本 + 内容哈希不匹配时给出明确提示而不是崩溃；④ 内容（卡/关卡/剧本）变更可在不重发客户端的前提下生效 |
| 4 | 删除冗余文档和无用脚本 | ① 根目录只留 README + 本重构文档；② `docs/` 为唯一现行规范，`docs/archive/` 存历史；③ 审计确认的 37 个死函数 / 12 个死常量 / 死信号 / 死后门全部清除；④ `git ls-files` 中不再有构建产物与运行日志 |

---

## 2. 现状基线（实测，作为"重构前"快照）

### 2.1 规模

| 目录 | 文件 | 行数 | 占比 |
|---|---|---|---|
| `ui/` | 46 | 16,330 | 53.5% |
| `core/` | 36 | 7,572 | 24.8% |
| `scripts/` 根 | 11 | 3,778 | 12.4% |
| `effects/` | 32 | 837 | 2.7% |
| `ai/` | 9 | 685 | 2.2% |
| `abilities/` | 15 | 613 | 2.0% |
| `actions/` | 10 | 373 | 1.2% |
| `net/` | 2 | 254 | 0.8% |
| `objectives/` | 2 | 60 | 0.2% |
| **合计** | **163** | **30,502** | 100% |

1,594 个函数 / 669 个常量 / 0 个测试 / 0 个 `assert`。服务端 `server/*.go` 约 682 行，纯转发，零游戏规则。

### 2.2 四类结构性问题（附证据）

**A. 规则与表现不分层**
- `cell.gd`(415 行) 是 `extends Panel`，既是棋盘数据单元又是 UI 控件；`BoardModel.grid_cells` 直接存 Cell 节点，连锁读写点约 35 处。
- `core→ui` 有 **7 条真实代码边**：`core/board_orchestrator.gd:439/449/471` 直接 `new()` 出 3 个 UI 类、`core/dialogue_manager.gd:64`、`core/play_controller.gd:13`、`core/quit_confirm.gd:88`。
- `core/` 内 `await` 115 次、`get_tree()` 18 次；`turn_system.gd`(1065 行) 55 处 `await`，规则推进挂在场景时钟上。
- 行动顺序依赖 UI 像素坐标：`core/board_registry.gd:74-78` → `core/board_slot.gd:153-162`（`bg_panel.global_position.x`）→ `core/turn_system.gd:338,350`。

**B. 隐式耦合与静默失效**
- 11 个 autoload；`core/` 内 **248 处** `Game.` 全局引用（横跨 20 个文件）、21/36 个 core 类 `extends Node`。
- **54 处 `has_method`** + **596 处字符串字典键**（237 个不同 key）；其中 13 处是规则代码反向探测 UI 方法（`cell.has_method("_update_hp_labels")` 之类）——重构时不会报错，只会静默失效。
- **805 处静默早退**（守卫失败直接 `return`，3 行内无任何日志）；`is_instance_valid` 345 次，`turn_system.gd` 单文件 40 次。

**C. 复制粘贴**
- `main.gd`(861) ↔ `test_main.gd`(1387)：**553 行逐字相同**（占 main.gd 有效行 86.5%），Jaccard 0.533；`Main.tscn`/`TestMain.tscn` 是 18 节点逐行克隆。
- `ui/changbanpo.gd` ↔ `ui/weizhenhuaxia.gd`：各 490 行，**仅 9 行不同**（98.2%）。
- 3 份侧栏面板管理器 / 3 份轮播 / 4 份滚动工具（5 个函数 ×4 份 = 20 份副本）/ 8 份手写 `to_dict`。
- 205 组同名函数覆盖 53.4% 的函数定义。
- 装配层：三段 bootstrap（`game_context.gd:302-415` / `:443-563` / `:581-714`）有 5 处"同一件事写三遍"。

**D. 数据驱动不彻底 + 零安全网**
- 关卡在场景脚本里合成（`test_main.gd:938-1104`），演义模式在 core 里合成（`game_context.gd:443-563` 硬编码卡名与血量 `:469,:480,:494,:509,:510`）。
- `effects/`、`abilities/` 参数类型标注率仅 11.1% / 32.1%。
- 无测试、无 CI；`128/163` 个脚本没有任何静态引用，全靠 autoload + 目录扫描 + `class_name` 在运行时汇合。

### 2.3 反作弊现状（威胁模型基线）

| 现状作弊面 | 证据 | 严重度 |
|---|---|---|
| 伪造 `disconnect/notify` → 全体替他执行 `damage_hero(100)` 秒杀 | `server/hub.go:348-388` 转发不校验 type；`test_main.gd:1298-1319` | 灾难 |
| 伪造 `game/end` → 宣告自己获胜，服务器立即销毁房间 | `server/hub.go:360-364`；`test_main.gd:1320-1335` | 灾难 |
| 伪造 `game/start` → 重定义整局（牌组/英雄/行动顺序/棋盘布局） | `server/hub.go:357-359` 只置 `Started=true`，不校验房主 | 灾难 |
| 伪造法术结算终态 `result_atk`/`result_health`/`result_cleared` | 发送 `core/play_controller.gd:215-228`；接收无条件写入 `:424-442` | 高 |
| 冒充他人结束回合（身份取自 payload 而非 `from`） | `test_main.gd:1345-1349` | 高 |
| 预知对手抽牌顺序（种子 + 全部牌组广播给全房） | `ui/sparring_panel.gd:1334-1358`；`core/game_context.gd:629-644` | 高（同时是玩法问题：没有暗牌） |
| 战斗结算无任何对账（锁步各自模拟，分叉即永久 desync） | `core/turn_system.gd:189-233`；伤害/死亡无同步消息 | 高 |
| 3v3/1v3 英雄技能只镜像给"第一个对手" | `ui/hero_action_bar.gd:184-198` → `core/play_controller.gd:340-346` | 中（现网 bug） |

---

## 3. 目标架构（目标 1：结构健康 + 扩展性）

### 3.1 目录与分层

```
nsoc/
├─ dev_gd/nsoc/                  # Godot 工程（客户端与服务端共用同一工程）
│   ├─ core/                     # 【共享·纯规则】无 UI、无场景树依赖、可 headless 运行
│   │   ├─ battle/               #   battle_session / turn_system / combat_system / board_model / board_slot
│   │   ├─ rules/                #   effects / abilities / actions / objectives（数据驱动单元）
│   │   ├─ data/                 #   data_loader + schema 校验 + 内容哈希
│   │   ├─ persist/              #   snapshot_io / deck_storage / 存档
│   │   └─ net/                  #   协议编解码 + 消息 schema（客户端服务端共用）
│   ├─ client/                   # 【仅客户端】ui/ + 场景控制器 + 输入/动画/主题
│   │   ├─ ui/                   #   面板、徽章、轮播、滚动工具（统一基类）
│   │   └─ scenes/               #   场景装配器（原 main.gd / test_main.gd / main_menu.gd）
│   ├─ server/                   # 【仅服务端】headless 入口：房间/对局驱动（复用 core/）
│   ├─ shared/                   # 版本常量、协议版本、内容哈希、模式枚举
│   ├─ data/                     # 【共享】全部内容数据（卡/英雄/关卡/战役/演义剧本/AI 牌表）
│   ├─ scenes/                   # .tscn（视图组合，节点用 %unique_name）
│   └─ tests/                    # headless 断言测试
├─ server/                       # Go 网关（房间/匹配/鉴权/进程守护），保留现有实现并扩展
├─ tools/                        # 离线工具（含 empire_map_editor.py）
└─ docs/                         # 现行规范；docs/archive/ 存历史设计稿
```

### 3.2 分层禁令（写进 CI，自动检查）

| 层 | 禁止出现 |
|---|---|
| `core/` | `Control` `Panel` `Node2D` `add_child` `create_tween` `get_tree()` `get_node(` `await ...timer` `/root/` `has_method(` 以及任何 `class_name` 属于 `client/` 的类型 |
| `client/` | 不得被 `core/` 引用（反向依赖检查） |
| `server/` | 只允许依赖 `core/` + `shared/` + `data/`，不得依赖 `client/` |
| 全局 | 新增 autoload 需在本文档登记；现有 11 个在阶段 2 收敛到 ≤5 个 |

> 说明：Godot 无头模式本身仍提供 SceneTree，所以**服务器权威并不强制先完成分层**（见 §7 阶段 1 可在分层前落地）。分层是让服务器更省资源、让规则可单测、让扩展更便宜的手段，不是权威化的前置条件。

### 3.3 扩展点（目标 1 的落点）

| 想扩展什么 | 只需改哪里 |
|---|---|
| 加一张卡 | `data/all_cards.json`（+ 需要新机制时加一个 `core/rules/effects/xxx.gd`） |
| 加一个效果 / 英雄技能 / 关卡动作 | 对应目录新增一个 `extends Effect/...` 的 RefCounted 脚本 + 显式注册表一行 |
| 加一个关卡 / 战役章节 | `data/chapters/*.json`（不再写脚本，取消 `changbanpo.gd` 这类每章一个脚本） |
| 加一个演义剧本 / 出征配置 | `data/empire/*.json`（从 `game_context.gd:443-563` 外提硬编码） |
| 加一种对局模式 | `shared/battle_mode.gd` 枚举 + `BattleSession` 一个 `start_xxx()` |
| 加一条网络消息 | `core/net/protocol.gd` 声明 + schema 版本号递增（服务器与客户端同时可用） |
| 加一个平台 | `export_presets.cfg` |

### 3.4 关键改造项（结构侧）

1. **目录归位**：`scripts/` 根目录的 11 个脚本（3,778 行）按职责拆入 `core/`（`card_*.gd`）、`client/ui/`（`cell.gd`、`hand_card.gd`）、`client/scenes/`（`main.gd`、`test_main.gd`、`main_menu.gd`、splash）。
2. **斩断 `core→ui`**：`board_orchestrator.gd:439/449/471`、`dialogue_manager.gd:64`、`play_controller.gd:13`、`quit_confirm.gd:88` 改为信号/接口注入。
3. **显式注册表**：`effect_registry.gd:16-53`、`action_registry.gd:18-48` 等的 `DirAccess` 目录扫描改为显式静态表（服务器无文件系统目录扫描也能装配）。
4. **`BattleSession`（新）**：合并三段 bootstrap 为一份，装配从场景脚本搬进 `core/battle/`；`BattleMode = CAMPAIGN | SKIRMISH | EMPIRE | PVP` 取代 `if Game.is_pvp` 与 `Game.pending_*` 全局字典。
5. **去重**：`main.gd`/`test_main.gd` 合一（`TestMain.tscn` 场景继承 `Main.tscn`）；三个章节脚本合一 + 数据化；3 份侧栏面板/3 份轮播/20 份滚动物理副本收敛为共享基类。
6. **数据/表现分离**：`Cell` 拆为 `CellData`(RefCounted) + `CellView`；`combat_system` 拆"纯结算函数 + 事件"。
7. **确定性收口**：RNG 集中可注入（含 `turn_system.gd:505/768/793`、`target_resolver.gd:45/99/130`、`effects/assault_charge.gd:38`）；行动定序改 `slot_index`；字典迭代序显式排序键。
8. **消除静默失效**：54 处 `has_method` → 类型化接口；13 处 `cell.has_method("_update_*")` → 信号；596 处字符串键 → 常量键；805 处静默早退补日志或断言。

---

## 4. 反作弊设计（目标 2：确保无法作弊）

### 4.1 核心原则

**服务器是唯一规则执行者。** 客户端只做两件事：把玩家操作变成"意图"发给服务器；把服务器返回的"权威结果"渲染出来。客户端不再计算任何影响胜负的数值。

这条原则同时满足目标 2 和目标 3：规则只有一份（服务器那份），客 户端即使版本落后也不会算错——因为它不算。

### 4.2 协议重设计

**上行（客户端 → 服务器）—— 只有意图，没有结果**

| 消息 | 载荷 |
|---|---|
| `intent/play_card` | `hand_index, slot_id, row, col`（**不含** `result_*`） |
| `intent/activate_equip` | `equip_index, target_slot_id, target_row, target_col` |
| `intent/activate_hero` | `ability_id, target...` |
| `intent/end_turn` | `seq`（**不含** `player_id`，身份由连接绑定） |
| `intent/cross_board` | `source_slot_id, target_slot_id` |
| `intent/choice` | `request_id, option_index`（回应服务器的选择请求） |
| `intent/surrender` | — |

**下行（服务器 → 客户端）—— 权威结果 + 表现事件**

| 消息 | 载荷 | 用途 |
|---|---|---|
| `auth/state` | 按玩家过滤的对局视图（自己手牌明文、对手手牌只给数量） | 状态同步/重连 |
| `auth/event` | 权威事件流（部署/移动/攻击/伤害/死亡/抽牌/弃牌…） | 客户端据此播动画 |
| `auth/request_choice` | `request_id, kind, options` | 替代现在"效果里 `await` 玩家 UI"（`core/effect_context.gd:227-236`） |
| `auth/verdict` | `winner_team, reason` | 胜负（客户端不再自己判定） |
| `auth/reject` | `reason, seq` | 非法意图拒绝（客户端回滚本地预览） |

清单一律删除：`result_atk`、`result_health`、`result_cleared`、`action/end_turn` 里的 `player_id`、`game/end`、`disconnect/notify`（改为服务器专属）。

### 4.3 服务器持有的唯一真相

- 洗牌种子与**所有玩家的牌堆顺序、手牌**
- 全部棋盘状态、英雄血量、费用、装备耐久、计数器
- 回合指针与阶段、随机源（单一可注入 RNG）
- 胜负判定、断线判定、超时判定
- `uuid → slot` 绑定（连接建立时分配，之后不可变更）

### 4.4 逐条封堵（对应 §2.3）

| 作弊面 | 封堵手段 |
|---|---|
| 伪造 `disconnect/notify` / `game/end` | 这两个 type 设为**服务器专属**：客户端发包一律丢弃并记日志；阵亡与胜负只由服务器产生 |
| 伪造 `game/start` | 对局初始化（牌组、英雄、种子、行动顺序、棋盘布局）由服务器生成并下发；客户端上报的只是"我选了哪套牌组/哪个英雄" |
| 伪造 `result_*` | 协议中删除该字段；服务器结算后以 `auth/event` 下发 |
| 冒充他人结束回合 | `player_id` 从协议删除，身份取连接绑定；每条意图带 `seq`，服务器校验行动者与序号 |
| 预知对手牌序 | 种子只在服务器；牌组内容只回显给本人；对手手牌仅同步数量 |
| 改客户端改数值 | 所有结算在服务器；客户端篡改仅影响自己的显示 |
| 重放 / 刷包 | 意图带单调 `seq`；服务器限速（如 10 条/秒）+ 连接数限制 |
| 版本不一致 | 握手校验 `PROTOCOL_VERSION`（不兼容即拒）+ `CONTENT_HASH`（数据不一致给出明确提示） |

### 4.5 明确的能力边界（不做无法兑现的承诺）

服务器权威能消除"改状态"，以下三类**不属于本方案范围**，需要单独机制：

1. **信息泄露**：观战/抓包看到不该看的信息 → 靠"按玩家过滤的视图"最小化（本方案已含）；观战模式需单独设计延迟视图。
2. **DDoS / 高频刷包**：靠限速与连接上限缓解（阶段 3 做基础版），完整防护属运维范畴。
3. **多开小号**：服务器权威后 `uuid → slot` 绑定即可；但**必须保留"同机多开"作为 dev 开关**（当前用 `session_id = uuid + 随机后缀`，见 `net/network_manager.gd:43-45`），否则无法本机联调。

---

## 5. 双端更新流程（目标 3：更新简单）

### 5.1 为什么现在简单（这是选 Godot 无头服的最大收益）

规则层（`core/`）与内容（`data/`）**只有一份**，客户端与服务端引用同一份代码与数据。因此：

- 改玩法 = 改一次，两端同时生效，不存在"客户端改了服务端忘了改"。
- 服务端权威 ⇒ **客户端算错也不影响权威状态**，因此两端版本可以有短暂差异而不炸局。

### 5.2 三件产物与版本三件套

| 产物 | 说明 |
|---|---|
| `NSOC-Client-Android.apk` | 客户端（正式） |
| `NSOC-Client-Windows.exe` | 客户端（开发调试用） |
| `NSOC-Server-Windows.exe` | 无头权威服（Windows，与服务端部署一致） |

| 版本标识 | 变化含义 | 不匹配时的行为 |
|---|---|---|
| `PROTOCOL_VERSION` | 消息结构变更 | 服务器拒绝连接，客户端提示"请更新到最新版本" |
| `CONTENT_HASH` | `data/` 全部 JSON 的内容哈希 | 服务器提示内容不一致（不炸局，可继续或拒绝，见 §5.4） |
| `BUILD_ID` | 构建标识 | 仅用于日志与排障 |

### 5.3 发布流程（一条命令）

```
1. 改代码/数据 → 本地 headless 跑测试（tests/）
2. 构建：导出三件产物 + 生成 version.json（三个版本标识）
3. 部署服务器：替换 NSOC-Server-Windows.exe 与 data/，重启守护进程
4. 客户端：Android 分发新包（或命中热更，见 §5.4）
5. 服务器保留兼容上一 PROTOCOL_VERSION 的能力 N 天（过渡期）
```

配套：Windows 服务端用 Go 网关 `server/` 做**进程守护 + 房间分配 + 鉴权 + 日志轮转**（网关可以拉起/重启 N 个 headless 对局进程，并在崩溃后清理房间）。

### 5.4 内容热更（第二阶段）

因为卡/关卡/剧本全在 `data/`：

- 第一步（阶段 4）：内容随包发布，启动时校验 `CONTENT_HASH`，不一致给出明确提示。
- 第二步（冻结后）：服务器作为内容源，客户端按哈希拉取增量内容包 → **加卡、调数值、改关卡不需要重发 APK**。这是把"更新简单"做到极致的关键一步。

### 5.5 回滚

- 服务器：保留上一版 exe + 内容包，回滚 = 换回文件 + 重启。
- 客户端：Android 保留上一版安装包；`PROTOCOL_VERSION` 兼容窗口期内旧客户端仍可玩。

---

## 6. 清理清单（目标 4：删冗余文档与无用脚本）

### 6.1 文档

| 现有文件 | 处置 | 理由 |
|---|---|---|
| `skills.md`（128KB） | 拆解重写为 `docs/ARCHITECTURE.md` + `docs/DATA.md`，原文移 `docs/archive/` | 是架构总结，仍有价值但混了历史状态 |
| `multiplay_dev_list_skills.md`（93KB） | 协议部分重写进 `docs/PROTOCOL.md`，原文移 `docs/archive/` | 联机决策与协议的唯一来源 |
| `multiplay_chess_skills.md` | 实现摘要并入 `docs/ARCHITECTURE.md`（多盘/1v3/3v3），原文移 `docs/archive/` | 功能已实装 |
| `dev3v3.md` | 移 `docs/archive/` | 已实装（文件自述为设计稿） |
| `ai_dev.md` | AI 章节并入 `docs/ARCHITECTURE.md`，原文移 `docs/archive/` | 设计稿，实装后有差异 |
| `turn_order_indicator_spec.md` | 移 `docs/archive/` | 已全部实装 |
| `step8_content.txt` | **删除** | 文件自述内容已完全并入主文档，无独有小节 |
| `README.md` | **保留**（版本更新记录），新增指向 `docs/` 的索引 | 是项目门面与更新日志 |

新建：`docs/ARCHITECTURE.md`、`docs/PROTOCOL.md`、`docs/ROADMAP.md`（本文件的落地版）、`docs/ARCHIVE-INDEX.md`（归档说明）。

### 6.2 代码（删除项均有审计依据，不涉及功能）

| 类别 | 具体 |
|---|---|
| 死文件 | `ai/net_action_sink.gd`（33 行，唯一引用是自己的 `class_name`） |
| 死函数（37 个） | `cell.gd:271 receive_damage`、`core/board_model.gd:51/68/71/83`、`core/turn_system.gd:59/72/87/126`、`core/effect_registry.gd:58 get_effect`、`core/hero_state.gd:55 heal`、`core/game_context.gd:97 is_player_alive`、`net/network_manager.gd:123/145` 等 |
| 死常量（12 个） | `cell.gd:55/56/57 ORIGIN_*`、`core/orientation.gd:26 ABS_DIRS`、`core/empire_save_storage.gd:29 MANUAL_SLOTS`、`ui/sparring_panel.gd:38/39 DEFAULT_HOST/PORT` 等 |
| 死信号 | `cards_loaded`、`level_loaded`（全仓零订阅，每次 bootstrap 白发） |
| 死代码块 | `main.gd:510-518 _collect_main_enemy_nodes`（零引用）；`_on_surrender` ↔ `_on_pvp_surrender` 同体异名保留一个 |
| 调试后门 | `main.gd:842-861 _dev_damage_*`、`_input` 里的 Ctrl+Q/W、F5/F9 快照热键 → 迁入独立的 dev cheats 脚本（`dev/` 目录，正式导出不含） |
| 幽灵注释 | 3 处 `pvp_lobby.gd` 引用（`test_main.gd:1213`、`core/game_context.gd:677`、`net/network_manager.gd:135`）——该文件不存在 |
| **保留** | `core/deck_manager.gd:112 to_dict_public()`：现在无调用点，但**服务器权威的暗牌视图要用它**，不删 |

### 6.3 仓库卫生

- `.gitignore` 补充：`*.exe`、`*.log`、`__pycache__/`、`*.pyc`、`dev_gd/nsoc/exports/`（现有仅忽略 exports）。
- `git rm --cached`：`server/nsoc-server.exe`（8.8MB）、`server/server.log`（含过期 panic 堆栈）、`dev_gd/tools/empire_map_tool/__pycache__/*.pyc`。当前 `.git` 已 32.7MB。
- `dev1/`（旧 JS 原型）：**保留原位**，在 README 与 `docs/archive/` 标注"历史原型、不再维护"。理由：删除无收益，且它是旧仓库 `soc` 的唯一对照物；符合"保留已有功能"的保守原则。（若你希望彻底清掉，评审时告知，我会移入 `legacy/` 或删除。）
- 已合并的 4 个远端旧分支（`multiplayer_branch`、`multiplayer_1v3_branch`、`branch_3v3`、`multi-chessboard-branch`）：README 已声明废弃 → 评审后删除远端分支。

---

## 7. 实施计划（目标 4 周冻结窗口 + 之后增量）

### 阶段 0：准备（1~2 天）

- 建 `refactor` 分支；对当前 `main` 打 tag `pre-refactor`。
- CI 骨架：headless 导入 + 脚本解析检查 + 分层规则检查（§3.2）+ 内容 JSON schema 校验。
- 安全网第一步：headless 驱动跑完一局并输出**状态哈希**（复用 `SnapshotState`/`SnapshotIO`，扩展到全 slot/全玩家）。
- **验收**：CI 能对每个提交给出"能否编译/是否违反分层/一局状态哈希"三项结论。

### 阶段 1：反作弊止血 + 权威化骨架（第 1 周，约 5~8 人日）

> 前置说明：Godot 无头模式仍提供 SceneTree 与 autoload，**权威化不依赖阶段 2 的分层**，因此先做这一阶段可以最快兑现"无法作弊"。

- **L0 止血（服务端，2~3 人日）**：`disconnect/notify`、`game/end` 设为服务器专属；`game/start` 只允许房主（`server/hub.go:357-359`）；转发时校验/重写身份字段；加 `seq` 与限速。
- **权威骨架（3~5 人日）**：新增 `dev_gd/nsoc/server/` headless 入口，跑通最小闭环——服务器初始化对局（自己生成种子与洗牌）、接收 `intent/play_card`、服务器结算、下发 `auth/event`；客户端删除 `result_*` 的发送与采信。
- **验收**：① 用篡改过的客户端发包（伪造断线/判胜/改结果）→ 服务器拒绝且其他玩家状态不变；② 抓包看不到对手手牌；③ 服务器与客户端的棋盘状态哈希一致。

### 阶段 2：结构归一（第 2~3 周，约 10~14 人日）

- 目录归位（§3.4-1）、斩断 `core→ui`（§3.4-2）、显式注册表（§3.4-3）。
- `BattleSession` + `BattleMode`（§3.4-4）；演义接口改造（硬编码关卡外提 + 6 个判定点改回调）。
- 去重：`main.gd`/`test_main.gd` 合一、三章节脚本合一、3 份面板管理器/3 份轮播/20 份滚动副本收敛（§3.4-5）。
- 测试网：把审计确认的 11 个纯逻辑文件 + `target_resolver` 转为 `RefCounted` 并补断言测试。
- **验收**：`main.gd` 与 `test_main.gd` 重复行从 86.5% 降到 <20%；`TestMain.tscn` 不再独立存在；`changbanpo`/`weizhenhuaxia` 合并为一个脚本 + 两份 JSON；分层检查通过；新增一关只改 JSON（实测演练一遍）。

### 阶段 3：分层深化 + 反作弊收口 + 清理（第 4 周，约 8~12 人日）

- `Cell` → `CellData` + `CellView`；`combat_system` 结算/动画分离；`turn_system` 拉直 `await`；RNG 集中；行动定序去 UI 坐标（§3.4-6/7）。
- 反作弊收口：`PROTOCOL_VERSION` + `CONTENT_HASH` 握手校验、`seq`/限速、`auth/request_choice` 取代效果里的 UI `await`、按玩家过滤视图（接线 `to_dict_public()`）。
- 清理：§6 全部执行（文档归档、死代码删除、`.gitignore`、`git rm --cached`）。
- **验收**：四项验收标准（§1）逐条通过；`core/` 零 UI 符号、零 `has_method`。

### 阶段 4：冻结后增量（不占窗口）

断线重连/重同步、对局进程守护与崩溃恢复、内容热更（§5.4 第二步）、观战延迟视图、测试覆盖补齐、`docs/` 完成重写。

### 工作量汇总（单人全职等效人日）

| 阶段 | 人日 |
|---|---|
| 0 准备 | 1~2 |
| 1 止血 + 权威骨架 | 5~8 |
| 2 结构归一 | 10~14 |
| 3 分层 + 收口 + 清理 | 8~12 |
| **冻结期内合计** | **24~36**（4 周 ≈ 20 人日，**预计会略微超窗口，优先保阶段 1 与阶段 2 的核心项**） |
| 4 增量（含完整权威化剩余） | 25~50 |

> 若冻结窗口必须严格控制在 4 周：**优先顺序为 阶段 1 → 阶段 2 的去重与 BattleSession → 阶段 3 的清理**，把"分层深化"与"反作弊收口"整体挪到增量阶段（权威化在阶段 1 已可用，收口属加固）。

---

## 8. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 冻结期超期 | 长时间不能出新内容 | 每阶段结束打 tag，可独立回退；阶段间不互相阻塞；新增卡牌数据工作可并行（不碰重构文件） |
| 锁步 PVP 的过渡态 | 阶段 1~3 期间旧 PVP 仍需可玩 | **锁步代码保留到阶段 3 完成后再删**，阶段 1 只在服务器侧加固 |
| `cell.gd` 解耦连锁（约 35 处读写点） | 回归风险最高 | 放在阶段 3；此前先有状态哈希对账兜底；改完用"同种子同输入 → 哈希一致"验证 |
| 静默失效（54 处 `has_method`） | 重构"看起来编译通过、运行时失效" | 阶段 2 起 CI 禁止新增 `has_method`；每阶段跑一局全流程冒烟（含 1v3/3v3/演义） |
| Windows 服务端进程管理 | 对局进程崩溃/残留 | Go 网关做守护与房间清理；先做"一局一进程 + 崩溃回收"，后续再做进程内多局（需先完成分层与依赖注入） |
| 内容热更的安全性 | 校验绕过 | 内容包带签名/哈希，服务器校验后才下发（阶段 4） |
| 安卓端包体与性能 | 体验下降 | 客户端不再需要完整结算逻辑，可裁剪；阶段 3 后评估包体 |

### 明确不做的事（避免范围蔓延）

1. **不把规则改写成 Go**（每张新卡要写两遍、永久双份维护）。
2. **不重写 UI 层**（16,330 行与底层重构正交，仅做 §3.4-5 的去重）。
3. **不动演义战略层**（`empire_test.gd` 1363 行、`yanyi_panel.gd` 696 行、13 个 `empire_*.gd`、`empire_map_editor.py`）——只改它与战斗底层的 6 个接口点。
4. **不动 `dev1/` 与 `tools/`**（历史原型与离线工具）。
5. **不追求 100% 测试覆盖**（只覆盖纯规则与协议编解码）。

---

## 9. 我替你决定的默认项（评审时可直接否决）

| # | 默认决定 | 说明 |
|---|---|---|
| 1 | `dev1/` 保留原位 + 标注废弃 | 若你要彻底清掉，改为移入 `legacy/` 或删除 |
| 2 | 远端 4 个旧分支在评审后删除 | 已合并进 `main` |
| 3 | 保留"同机多开"能力，但作为 dev 开关 | 否则无法本机联调 PVP |
| 4 | 上帝对象 `Game` 保留名字，但缩减为"上下文持有者" | 阶段 2 把规则搬进 `BattleSession`，避免一次性改名引发全库改动 |
| 5 | `effects/` 的 44 个纯元数据桩脚本**保留** | 它们是"卡面说明文本"的来源，客户端要用；只清理真正无用的 |
| 6 | 内容热更分两步（先随包校验、后增量拉取） | 冻结期内只做第一步 |
| 7 | 服务端与 Go 网关同机部署（Windows） | 若需分离部署，阶段 4 再拆 |
| 8 | `to_dict_public()` 保留待接线 | 服务器暗牌视图要用 |

---

## 10. 附：验收检查表（开工后每个阶段结束逐条勾）

- [ ] CI 通过：脚本解析 / 分层规则 / JSON schema / 一局状态哈希
- [ ] `core/` 内 `Control|Panel|Node2D|add_child|create_tween|get_tree()|has_method` 计数 = 0
- [ ] `core/` → `client/` 反向依赖边数 = 0
- [ ] 篡改客户端冒烟：伪造断线 / 判胜 / 改结果 / 冒充回合，全部被拒且不影响他人
- [ ] 抓包验证：对手手牌与牌堆顺序不可见
- [ ] 同种子 + 同输入 → 状态哈希一致（跨进程、跨窗口尺寸、跨运行顺序）
- [ ] 新增一张卡 = 只改 `data/all_cards.json`（演练一次并记录）
- [ ] 新增一关 = 只加一个 JSON（演练一次并记录）
- [ ] 一条命令产出三件产物 + `version.json`
- [ ] `git ls-files` 无 `*.exe` / `*.log` / `*.pyc`
- [ ] 根目录只剩 `README.md` + `重构文档.md` + 工程目录
- [ ] 死代码清单（37 函数 / 12 常量 / 死信号 / 死后门）全部清除且无回归
- [ ] 1v1 / 1v3 / 3v3 / 战役 / 演义 五条路径全流程冒烟通过

---

## 11. 实施进度记录

### 阶段 0：准备与安全网 —— ✅ 已完成

| 交付物 | 说明 |
|---|---|
| `pre-refactor` tag + `refactor` 分支 | 基线固定，所有重构提交落在 `refactor` 分支 |
| `tools/ci/check_layers.py` + `layer_baseline.json` | 分层棘轮检查：纯规则层禁止 UI/场景树/Tween/定时器/反射/资源路径；当前基线 **181 处 / 78 个文件-规则对**，只减不增 |
| `tools/ci/check_content.py` | 卡牌/英雄/关卡/演义地图的结构与交叉引用校验（含效果脚本、技能脚本、关卡动作、场景路径、坐标范围、重名） |
| `tools/ci/run_headless_smoke.ps1` | 三层保护的 headless 冒烟：预热导入 → 跑一局 → （`-Verify`）跑两次比对逐回合哈希 |
| `dev_gd/nsoc/tests/StateHash` + `HeadlessBattle` | 状态哈希工具（只含规则状态、键排序、含牌堆顺序）+ 复用真实 `Main.tscn` 装配的冒烟场景 |
| `dev_gd/nsoc/data/chapters/smoke_test.json` | 冒烟专用章节（固定牌堆 + 无触发器 + 长回合目标），任何数值改动都会改变哈希基线 |
| `.gitignore` | 补全构建产物、Godot 缓存、Python 缓存 |

**验收结果**：冒烟通过（3 回合），确定性已验证到三个维度——① 同一副本连跑 3 次；② 两个独立副本各自导入后各跑一次；③ 均逐回合哈希完全一致。

当前哈希（信息性基线，**不是要 pin 的常量**；CI 断言的是"两次运行一致"，任何内容/代码改动都可能合法地改变它）：
```
BATTLE_MODE campaign
turn 0: 0aabf7a52b32ff06e540583ae2bc88de94d2168e0c542c1309c2233ef98ab95d
turn 1: 39c7af22c97b19ec162152e7aff2feabe8f1556a5e3b88fdf7a84ab3364f06c9
turn 2: 007f8d3bf9fd8eef764380dd59b50b2932bc4e38dc7c14d03ccbf983c972fbe1
turn 3: 652a3ef286eca42605d10065f817b3cf955ffcfab238417348729a3398331df8
STATE_HASH: 652a3ef286eca42605d10065f817b3cf955ffcfab238417348729a3398331df8
```
第二条黄金路径（多棋盘 PVE，`HeadlessTestBattle`，`--turns=2`）：
```
STATE_HASH: 0576fcc90d28747719b68bcb9cb40d79fbc25d08f780a7dda19fb1828995e0ce
```

> **基线修正记录**：上面单盘的字面值此前记为 `76d91c99… / d35d86d3… / 9e2f44dd… / cc675fe8…`（`cc675fe8` 是 **turn 3 的 `TURN_HASH`**，不是最终 `STATE_HASH`）。复核时发现该字面值**在任何后期提交上都不可复现**：`1c7f1af`（切片 9，即声称建立该基线的那次提交）、`f61cc62`、`3e9170c`、`9619756`、`7193dad`、HEAD 逐一导出成独立副本后跑 `HeadlessBattle`，全部给出 `turn0=0aabf7a5… / turn3=652a3ef2…`；换成本仓就地运行（`.godot` 预热）结果同样如此，说明与环境无关。最可能的原因是切片 9 在给 `StateHash` 补 `__local__`（本地牌堆别名）时，把**改动前**的值记进了文档。因此：
> - 以本节的值为准（已用"同一副本跑两次 + 两个独立副本各跑一次"验证一致）；
> - 各切片的**真正证据是"改动前后逐位相同"**（同一次复核里 HEAD 基线与工作树在两条路径上逐位一致），字面值只作参考，不要当成必须复现的常量；
> - 多棋盘 `0576fcc9…` 与当时记录一致，未受影响。

**尚未完成（转入后续阶段）**：① 跨窗口尺寸的哈希一致性——**定序去像素已实装**（见阶段 2 切片 8），由单元测试证明（测试不创建任何 `bg_panel`，旧实现只能返回 `INF` 导致顺序随机，新实现给出确定顺序）；headless 下两种分辨率的哈希对比只能作为回归检查，**带窗口的多棋盘集成测试仍是待办**。② CI 工作流需在 GitHub 上首次实跑验证（本地无法执行 ubuntu runner）。

### 阶段 0 踩坑记录（重要，后续会再遇到）

1. **全新副本必须先 `--import` 预热**。没有 `.godot/global_script_class_cache.cfg` 时，所有 `class_name` 类型引用都会解析失败，报出上百条 `Could not find type "X" in the current scope`、`Identifier not declared`，以及"autoload 脚本不继承 Node"的连带错误——**全是假报错**，看起来像项目崩了。
2. **协程 `run()` 不能取返回值**。`Game.turn.run()` 是 `-> void` 协程，写 `var s = Game.turn.run()` 会直接 Parse Error（`Cannot get return value of call to "run()"`）；必须 `await Game.turn.run()`。若要超时控制，用**独立协程**做心跳/看门狗，而不是包 lambda（lambda 的协程状态无人持有会被释放，完成标志永不置位）。
3. **harness 脚本解析失败 = Godot 静默空转**。场景根节点没挂上脚本时 Godot 不会退出；脚本内看门狗也在同一个坏脚本里，因此外层必须有进程级 `WaitForExit` 超时 + `Kill` 兜底。
4. **Windows PowerShell 5.1 把无 BOM 的 UTF-8 当 GBK**。`.ps1` 里写中文会导致解析失败；本项目 `.ps1` 一律保持纯 ASCII（中文说明放本文件/doc），`.py` 与 `.gd` 用 UTF-8 无妨。
5. **PS 5.1 的 `[ordered]@{}` 不能用整数做键**（会被当成位置索引并抛 `ArgumentOutOfRangeException`），改用普通 `@{}`。
6. 工具链事实：本机 **未安装 Go**（`server/nsoc-server.exe` 目前是唯一可运行的联机服务二进制，清理时不可先删）；Godot 位于 `C:\D\GodotEngine\Godot_v4.7.2-stable_win64_console.exe`；本机 `pwsh` 不存在，用 `powershell.exe`（5.1）。
7. **GDScript 运行时错误不会终止 `_ready()`**：某个测试函数因错误提前返回时，测试照常走到结尾并打印 PASS。本项目就踩过一次（`BattleAuthority._on_end_turn()` 声明返回 `Dictionary` 却返回 `Array` → 编译失败 → 后续 32 条断言全部静默跳过，结果仍报 4 passed / 0 failed）。对策：测试里加"预期用例总数"校验（`EXPECTED_CASES`），数量不符直接判 FAIL。

### 阶段 1：反作弊止血 + 服务器权威骨架 —— 🚧 进行中（GDScript 侧 + Go 中继侧已完成）

| 交付物 | 说明 |
|---|---|
| `scripts/core/net/protocol.gd`（`NetProtocol`） | 协议唯一真相：`VERSION=2`；`intent/*` 上行白名单、`auth/*` 下行、拒绝原因常量；`validate_intent()` 结构校验；`server_may_accept()` / `client_may_send()` 把 `game/start`、`game/end`、`disconnect/notify` 等列为**服务器专属**；`content_hash()` 供握手校验 |
| `scripts/server/battle_authority.gd`（`BattleAuthority`） | 服务器权威核心骨架：持有种子/抽牌堆顺序/手牌/费用/血量/回合指针；四道校验（身份、回合归属、序号单调、限速）；费用与手牌按服务器结算；**按玩家过滤的私有视图**（对手手牌只有数量）；权威事件流（抽牌事件只发本人）。刻意**不重新实现棋盘规则**，等阶段 2/3 接入同一套规则引擎 |
| `scripts/server/server_session.gd`（`BattleServerSession`） | **服务器入口层**：消息信封 → 协议校验 → 权威结算 → 按玩家路由。四个反作弊点：① 客户端发来的服务器专属消息（`game/end` / `disconnect/notify` / `game/start` / `auth/*`）**一律丢弃并记审计日志**；② 身份永远取连接绑定的 `sender_pid`，payload 里的身份字段被忽略；③ 协议版本不匹配拒绝、内容哈希不匹配告警；④ 未握手不得发意图。开局事件立即路由（不积压到第一条意图） |
| `tests/authority_test.gd` + `AuthorityTest.tscn` | 46 条反作弊断言（含 6 条注册表完整性），headless 运行 |
| `tests/server_session_test.gd` + `ServerSessionTest.tscn` | 32 条服务器边界断言，headless 运行 |

**验收证据**：`AUTHORITY_RESULT PASS passed=46 failed=0`、`SERVER_RESULT PASS passed=32 failed=0`，脚本错误 0；同时冒烟回归通过（原 PVE 路径未受影响）。
覆盖的断言：协议准入（含"客户端不得发送服务器专属消息"）/ 身份 / 回合归属 / 序号重放 / 结构校验 / 旧协议 type 被拒 / 手牌与费用 / 限速 / 私有视图不泄漏对手手牌 / 抽牌事件不下发给对手 / 回合推进与费用曲线 / 投降与终局 / 服务器 RNG 同种子可复现；**服务器边界**：未握手拒绝、协议版本不匹配、内容哈希告警、**伪造 `game/end` / `disconnect/notify` / `game/start` / `auth/verdict` / `auth/state` 全部被丢弃且不改变对局**（审计里的灾难级漏洞）、身份取连接绑定（payload 冒充无效）、私有事件不外流、队列 drain 语义。

**本阶段尚未完成**：
1. **客户端接入**：`TestMain` 仍在走 v1 的 `action/*` 路径；切换到权威路径需要先有可用的服务器侧棋盘结算，否则会破坏 PVP 可玩性。（结算画面本身已不再信任对端消息 —— 见阶段 3 的"结算不再信任对端消息"。）
2. ~~**Go 侧止血**~~ ✅ **已完成**（见阶段 3 表格"中继层止血"）：type 白名单、`game/start` 房主校验、身份字段重写、每连接限速均已落地并有 Go 单测。`seq` 单调校验留在权威协议路径（v1 转发层不做，避免破坏旧客户端）。
3. **棋盘战斗结算接入权威端**：依赖阶段 2/3 的规则/表现解耦。

### 阶段 1 待办（下一步）

1. ~~服务端止血~~ ✅ 已完成（`server/security.go` + `security_test.go`）。
2. 权威骨架：新增 `dev_gd/nsoc/server/` headless 入口，跑通"服务器生成种子与洗牌 → 收意图 → 服务器结算 → 下发权威事件"最小闭环；客户端删除 `action/*` 的发送与采信。
3. ~~服务端相关改动落在 `server/*.go`~~ ✅ 已完成：Go 环境已就绪（`GOPROXY=goproxy.cn` + 临时 `GOCACHE`/`GOMODCACHE`/`GOTMPDIR` 即可在无管理员权限的 Windows 上构建与测试）。

---

### 阶段 2：结构归一 —— 🚧 进行中

已完成切片：

| 切片 | 内容 | 证据 |
|---|---|---|
| **显式注册表**（§3.4-3） | 四个注册表（`effect_registry` / `action_registry` / `hero_ability_registry` / `objective_registry`）去掉 `DirAccess` 目录扫描，改为**显式路径表** `*_PATHS`；新增 `ids()` 内省接口（字典序、确定性） | 效果 32 / 技能 15 / 关卡动作 10 / 关卡目标 1 全部注册；**冒烟 STATE_HASH 与改动前逐位一致**（`a6aa80de…`）→ 证明是行为不变的重构 |
| **CI 守卫** | `check_content.py` 新增"注册表 == 目录"严格校验（漏登记 / 登记了不存在的脚本都失败）；headless 测试新增 6 条注册表断言（数量 + 卡牌与英雄实际引用的 id 抽查） | 内容检查通过；权威测试 46/46 通过 |
| **场景装配冒烟**（测试网） | 新增 `tests/SceneLoadTest`：递归遍历 `res://scenes/**/*.tscn`，逐个 `load` + `instantiate` + `free`（**不入场景树**，避免章节面板 `_ready` 里的 change_scene 副作用），并校验三个章节场景提供 `chapter_name()` 契约 | 改动前：`SCENE_RESULT FAIL loaded=27 failed=3`（3 个章节缺契约）；改动后：`SCENE_RESULT PASS loaded=27 failed=0` |
| **章节脚本三合一**（§3.4-5） | 新增 `scripts/ui/chapter_panel_base.gd`（`ChapterPanelBase`，由 `changbanpo.gd` 逐字节复制后把 6 个分叉点改为可覆写接口：`chapter_name()` / `config_path()` / `log_tag()`）；`changbanpo.gd` 与 `weizhenhuaxia.gd` 各从 **490 行降到 22 行**；`jieting.gd`（有真实差异：上"马谡"/下"王平"双检阅面板）先补齐同名接口，待后续并入基类 | 净减 **427 行**；冒烟 `STATE_HASH` 逐位不变（`a6aa80de…`）；场景冒烟 27/27 |
| **行动定序去 UI 像素**（§3.4-7） | `BoardSlot.visual_x()`（读 `bg_panel.global_position.x`）→ `order_key()`（返回 `slot_index`，它本身就是布局空间顺序，`BoardLayoutResolver` 也用它决定左/中/右）；`BoardRegistry.sorted_by_x()` → `sorted_by_order()`（`slot_index` 升序，同值按 id 兜底）；`turn_system` 两处阶段遍历与 `board_orchestrator` 的行动顺序指示器统一改用同一逻辑键 | 规则层不再读取任何屏幕坐标；RulesTest 新增 2 条定序断言（测试**不创建任何 `bg_panel`**，旧实现只能返回 `INF`）→ **31/31**；两种分辨率冒烟哈希一致 |
| **规则随机源集中**（§3.4-7） | `Game` 新增 `battle_rng` + `pending_battle_seed` + `seed_battle_rng()` / `rand_index()` / `shuffle_in_place()`；PVP 由服务器下发的种子驱动，PVE 可用 `pending_battle_seed` 固定。替换掉规则层最后 7 处裸随机：`turn_system`（AI 选盘、自动跨盘攻击/移动目标）、`target_resolver`（3 处）、`assault_charge`（`candidates.shuffle()`）、`deck_manager` 的 PVE 洗牌（原 `draw_pile.shuffle()`） | 规则层**不再使用全局 `randi()` / `Array.shuffle()`**；RulesTest 新增 2 条"同种子 → 同序列/同洗牌"断言（31 → 33）；3 次冒烟逐回合哈希一致 |
| **安全网补强：哈希覆盖本地牌堆** | `StateHash` 原先只遍历 `Game.decks`（pid → deck），而 **PVE 下本地牌组只挂在 `Game.deck` 别名上**，导致洗牌结果根本不进哈希——牌堆顺序变化无法被冒烟发现（本轮改随机源时暴露） | 补上 `Game.deck` / `Game.mana` 别名后哈希基线更新（当时记为 `cc675fe8…`，**该字面值已被 §11 阶段 0 的"基线修正记录"证伪**，见上方"基线修正记录"），并保持 3 次一致；此后再有洗牌/发牌回归会被立刻发现 |
| **第二条黄金路径：多棋盘 PVE** | 新增 `tests/HeadlessTestBattle`（跑 `TestMain.tscn` 的**非 PVP** 分支：6 个盘 + AI 敌人 + 跨棋盘阶段），并内置**无头决策器**（订阅 `front_row_action_requested` 确定性选第一个敌队盘——即服务器将来用 `auth/request_choice` 做的事）。此前只有 `Main.tscn` 单盘战役一条路径被覆盖 | `BOOT_TESTBATTLE slots=6`、2 回合 3 次运行哈希一致（`0576fcc9…`）、6 回合稳定跑完；`FRONT_ROW_RESOLVED 0`（当前关卡下单位还没活到前排，决策器暂未被触发）；已接入 CI |
| **显式对局模式**（§3.4-4 第一步） | 新增 `scripts/core/battle_mode.gd`（`BattleMode.Kind`：CAMPAIGN / SKIRMISH / EMPIRE / PVP，附 `name_of` / `is_valid` / `default_uses_ai` / `is_authoritative` / `from_pending`）；`Game` 新增 `battle_mode` 字段，由三段装配入口显式设置；`main.gd` 不再自行推断 `_is_campaign`，改为读 `Game.battle_mode` | 冒烟打印并断言 `BATTLE_MODE campaign`；RulesTest 新增 4 条模式断言（25 → 29）；四套测试全绿；`STATE_HASH` 逐位不变 |
| **目录归位**（§3.4-1） | `scripts/` 根目录的 11 个脚本按职责归位：4 个领域模型（`card_base` / `card_unit` / `card_spell` / `card_equipment`）→ `core/`；2 个视图节点（`cell.gd` / `hand_card.gd`）→ `ui/`；5 个场景控制器（`main` / `test_main` / `main_menu` / `splash_screen` / `video_splash`）→ 新建 `app/`；各自 `.uid` 一并移动以保持 uid 稳定 | 根目录 `.gd` **归零**；`core` 37 / `ui` 52 / `app` 5；7 处 `.tscn` 脚本路径同步更新；`import` 0 解析错误、**0 场景加载失败**；四套测试全绿；`STATE_HASH` 逐位不变 |
| **三段 bootstrap 去重**（§3.4-4 第一步） | 在 `game_context.gd` 内抽出三个共用步骤：`_reset_battle_subsystems()`（清 counters / 技能回合用量 / 装备 / 回合运行态，原 3 份副本）、`_load_card_db(cards=[])`（原 4 份副本）、`_clear_pending_inputs()`（原 3 份副本）；并删除 3 处 `cards_loaded` / `level_loaded` **死信号发射**与两条声明（全仓 0 订阅者） | `game_context.gd` 759 → 740 行；四套测试全绿；`STATE_HASH` 逐位不变（`a6aa80de…`）→ 行为不变 |
| **斩断 `core→ui` 依赖边**（§3.4-2） | ① 三个纯 UI 脚本搬出 core：`quit_confirm.gd`(149) → `ui/`、`dialogue_manager.gd`(142) → `ui/`、`board_orchestrator.gd`(714) → `ui/`（都只被 autoload 名或 app 层按类型引用，故只改 `project.godot` 的两条 autoload 路径）；② `play_controller.gd` 的 `var hand_view: HandView` → `Node`；③ 规则层 4 处 `EffectBadgeFactory.refresh(...)` 改为**信号驱动**（客户端订阅 `cell.effects_changed`），规则层不再碰 UI | 分层违规 **181 → 122（−33%）**，其中 **`client-class-*` 与 `path-ui-script` 归零**；新增检查规则"共享层不得引用 ui/ 的 class_name"（机器可查）；`import` 0 解析错误 / 0 autoload 失败；四套测试全绿；`STATE_HASH` 逐位不变 |

为什么要做：服务器无头装配不应依赖文件系统目录枚举；扫描顺序不确定会带来注册顺序不确定性；显式表让"新增效果"成为一次可见改动，漏登记由 CI 拦下。

阶段 2 剩余待办（按风险从低到高）：

1. ✅ **章节脚本三合一**（已完成，见上表）：`changbanpo` / `weizhenhuaxia` 已并入 `ChapterPanelBase`；`jieting` 补齐了接口但尚未并入基类（它有真实差异，需在基类上留"检阅面板"钩子）。
2. ✅ **测试网（第一批）**：`SceneLoadTest` 覆盖"全部 27 个场景可 load + instantiate"；`RulesTest` 覆盖四个无需场景树的纯逻辑模块（`Orientation` 方向映射与血量视角、`ManaSystem` 费用曲线与封顶、`DeckManager` 抽牌/墓地回收/种子确定性、`MarkupParser` 标记转换），共 25 条断言；`AuthorityTest` 46 条。**待办**：`BoardModel` / `TargetResolver` / `CombatSystem` 等依赖 Cell 节点的模块，需在阶段 3 完成数据/表现分离后再补测试。
3. ✅ **斩断 `core→ui` 依赖边**（已完成，见上表）：`board_orchestrator` / `dialogue_manager` / `quit_confirm` 搬入 `ui/`；`play_controller` 的 `HandView` 类型依赖去掉；规则层的 4 处徽章刷新改为信号驱动。剩余的是"core 里仍在使用 Control/Tween/add_child"这类**能力**耦合（`turn_system` / `combat_system` / `board_slot_factory` / `play_controller` / `game_context`），属阶段 3。
4. ✅ **测试网**（见上表）。
5. ✅ **目录归位**（已完成，见上表）：`scripts/` 根目录归零，分层现在与目录一一对应（`core/` `rules/`（effects|abilities|actions|objectives）`server/` `ai/` `net/` `ui/` `app/`）。
6. **`BattleSession` + `BattleMode`**：✅ **显式模式已落地**（`BattleMode.Kind` + `Game.battle_mode`，`main.gd` 不再自行推断），三段 bootstrap 的重复也已消除。**剩余**：把三段装配提升为 `BattleSession` 类，让 `Game` 退化为纯上下文持有者；`pending_*` 字段改为参数传入（服务器侧 `start_pvp(config)` 已在 `BattleAuthority` 骨架中预留）。
7. **最大的一块**：`main.gd`(861) ⟷ `test_main.gd`(1387) 合一（553 行逐字相同、`Main.tscn`/`TestMain.tscn` 是 18 节点克隆）；以及 3 份侧栏面板管理器 / 3 份轮播 / 20 份滚动物理副本的收敛。

### 阶段 3：分层深化 + 反作弊收口 + 清理 —— 🚧 进行中（清理与定序先行）

| 切片 | 内容 | 证据 |
|---|---|---|
| **行动定序去 UI 像素**（§3.4-7） | 见阶段 2 切片 8（`visual_x()` → `order_key()`，规则层零屏幕坐标） | RulesTest 定序断言（不创建任何 `bg_panel`） |
| **规则随机源集中**（§3.4-7） | 见阶段 2 切片 9（`Game.battle_rng` + 7 处裸随机全部替换） | RulesTest 同种子断言；3 次冒烟一致 |
| **战斗结算与表现分离**（§3.4-6） | `CombatSystem.attack_cells()`（90 行，把伤害数学与 `play_damage_effect` / `_update_hp_labels` / `create_timer` 混写）拆成：纯静态 `resolve_attack(attacker, defender_data_list) -> 阵亡列表` + `is_cell_dead()`（**不碰场景树、不建 tween、不刷 UI**，服务器与单测共用同一份规则），`attack_cells()` 只保留表现与时序（含"攻击者只有真掉血才闪红"的等价处理） | RulesTest 新增 8 条结算断言（普通 / frail / soaked / yi_bing / 反伤致死 / 攻击者存活），由**不含任何表现方法的 `FakeCell`** 驱动 —— 结算若偷调 UI 会立刻报错 → **41/41**；两条黄金路径哈希逐位不变（当时的记录值 / `0576fcc9…`，见"基线修正记录"）→ 行为等价 |
| **死代码清理**（§6.2） | 新增一次性迁移工具 `tools/ci/strip_dead_code.py`（按符号名删除、保守规则、干跑/应用两段）；删除 **45 个已确认零引用的定义**（34 函数 + 11 常量，如 `receive_damage` / `iter_cells` / `by_team` / `register_extra_board` / `resolve_hero` / `damage_enemy_hero` / `ORIGIN_*` / `TARGET_BOARD_ID`）；删除死文件 `ai/net_action_sink.gd`（33 行）；修掉 3 处引用不存在的 `pvp_lobby.gd` 的幽灵注释 | 删除后**全仓残留引用统计 = 0**；import 0 解析错误；四套测试全绿；冒烟 `STATE_HASH` 逐位不变（当时的记录值，见"基线修正记录"）→ 证明删的确实是死代码 |
| **仓库卫生**（§6.3） | `.gitignore` 补全（阶段 0）；`git rm --cached server/nsoc-server.exe`、`server/server.log`、`__pycache__/*.pyc` —— **磁盘文件保留**，只是不再入库（`nsoc-server.exe` 仍是本机唯一可运行的联机服务二进制，待 Go 环境就绪后可从源码重建） | `git ls-files` 不再含 `*.exe` / `*.log` / `*.pyc`；`.git` 体积不再增长 |
| **中继层止血**（§4.1，Go） | Go 环境恢复后（`GOPROXY=goproxy.cn` + 临时 `GOCACHE`/`GOMODCACHE`）重建 `server/`，新增 `security.go` 落实四条已确认策略：① 服务器专属消息（`game/end`、`disconnect/notify`、`auth/*`、`room/*` 服务器推送）客户端发来**丢弃 + 记日志**；② `game/start` 只允许 `room.HostUUID` 发送；③ payload 里 `player_id` / `uuid` 一律改写为连接真实 uuid；④ 每连接 **20 条/秒**滚动窗口限速，超限丢弃（不断开） | `go build` / `go vet` 干净；新增 `security_test.go` **10 个用例全绿**（伪造 9 种服务器专属消息均未转发未改状态、非房主 `game/start` 被拒且房间 `Started` 不变、房主放行、5 条身份重写断言、限速配额与窗口滚动、普通 `action/*` / `room/deck_ready` 转发行为逐位不变）；客户端 v1 消息格式**零改动** |
| **结算不再信任对端消息**（§4.1 客户端侧） | 中继层拦下 `game/end` 后暴露出旧依赖：多队伍 PVP 只把本端主英雄 + `resolver.top_slot` 的 `hero.died` 接到结算 UI，我方其他英雄 / 敌方非 top 英雄阵亡时**只能靠收到的 `game/end`（含自己广播给自己的回声）**弹结算画面。改为**本端确定性推导**：`Game.pvp_end_game()` 先 `emit match_result_decided` 再广播；胜负计算收敛为 `Game.winning_team_for(loser_team)`（`board_slot` 不再内联）；`test_main` 新增 `_apply_match_result()` 作为统一结算入口（本端信号与兼容用的 `game/end` 共用）。同时发现**投降攻击不了**：`damage_hero(100, "triggered")` 是纯本地伤害，对端推算不出 → 投降改为显式网络消息 `action/surrender`（v2 对应名为 `intent/surrender`），对端按 `disconnect/notify` 同路径判其阵亡 | RulesTest **41 → 48**（3v3 双向、1v3、空/单队边界、`pvp_end_game` 在本端 emit 且非 PVP 不 emit）；六套 headless 全绿（Rules 48 / Authority 46 / Server 32 / Scene 27 / 两条黄金路径 PASS）；`server/security.go` 单测 10/10；**HEAD 基线与工作树在两条黄金路径上哈希逐位一致**（`652a3ef2…` / `0576fcc9…`）→ 证明改动对 PVE/战役路径零影响 |
| **数据/表现分离第一步：`CellData`**（§3.4-6） | 新增 `scripts/core/cell_data.gd`（`CellData extends RefCounted`）：把 `Cell` 的状态字段与 `set_card` / `set_phantom` / `clear_card` / `to_dict` / `from_dict` **逐条同构**地搬到纯数据对象上，仅两处刻意差异（不继承 Control；`team_id` 不再反查 `Game.registry` 而由调用方显式注入 —— 服务器没有客户端 autoload）。`BoardModel.register_cell()` 去掉 `: Node` 形参类型，`grid_cells` 现在既可装 `Cell` 节点也可装 `CellData`，`populate_initial_units()` 两个实现共用 | 新增 `tests/HeadlessBoardTest`（**21 条断言，纯数据、不加载任何 .tscn、不建 Control**）：3×3 装配、邻接敌对查询（team_id 与 PVE 回落双路径）、`CombatSystem.resolve_attack` 在 `CellData` 上结算、`reset_attack_flags`、`to_dict → JSON → from_dict` 往返（含 float→int 归一化与二次序列化逐键相等）；已接入 CI；**客户端零改动**，六套回归全绿且两条黄金路径哈希逐位不变 |
| **`Cell` 退化为视图**（§3.4-6，本切片核心） | `cell.gd` 的 17 个状态字段全部改成**转发属性**（`var has_card: bool: get: return data.has_card` …），状态真正存放在 `CellData` 里；`set_card` / `set_phantom` / `_do_clear` / `to_dict` / `from_dict` 改为"先写数据、再刷视图"（`_apply_card_view()` 承担原 set_card 的 UI 部分）。因此**规则层与 UI 的调用点零改动**（`cell.health["front"] -= dmg` 这类原地修改照旧生效，因为 getter 返回同一个 Dictionary 引用），而 `team_id` 的 `Game.registry` 反查按设计留在视图侧。顺带删掉重复的 `set_phantom` 定义与迁走后无用的 `Cell._int_dict()` | 新增 `tests/CellViewTest`（**19 条断言**，实例化真实 `scenes/Cell.tscn`）：转发双向无损（写 cell→落 data、写 data→读 cell、原地改子字典）、`set_card` 后状态与同参数 `CellData` **逐键相等**、四维标签按单位视角映射到正确绝对方向（`is_enemy=true` 时 bottom↔front）、`clear_card` 清状态 + 视图复位 + `cleared` 只发一次、`set_phantom` 数据/视图一致、`from_dict` 往返逐键相等、`CombatSystem.resolve_attack` 作用在 Cell 上时改的是 `CellData` 状态；已接入 CI。八套 headless 全绿（CellView 19 / Board 21 / Rules 48 / Authority 46 / Server 32 / Scene 27 / 两条黄金路径），**两条黄金路径哈希逐位不变**（`652a3ef2…` / `0576fcc9…`）→ 行为等价 |

| **无视图装配：服务器盘面可无头搭出来**（§3.4-6） | `board_slot_factory.gd` 拆成两层：`create_slot(...)` 成为**客户端与服务器唯一的装配真相**（BoardModel / HeroState / SpawnerSystem / SpellCasterSystem / BoardSlot / registry / 初始单位摆放），`use_view_cells` 只决定"格子怎么造"（`Cell` 节点 + `grid_node`，或 `CellData` 纯数据）；`create_main()` 保持原签名与顺序（cell 先建 → slot → 再补 `bg_panel`/`hero_panel`/`grid_node`），新增 `create_headless()`（零视图；PVP 的 `team_id` / `owner_player_id` 显式注入到全盘格子）。同时修掉一处真问题：`CombatSystem.move_card` 的瞬时（服务器）路径原来**不透传 `team_id`**，而 `CellData` 又没有 `Game.registry` 可反查 → PVP 单位一跨盘就掉队伍归属；现在两条路径一致透传（PVE 下 `team_id` 为空串，行为不变） | 新增 `tests/HeadlessSetupTest`（**20 条断言**）：3×3 全 `CellData`、`grid_node`/`bg_panel`/`hero_panel` **全为空**、英雄 / spawner / 法术施放器就位、registry 三查命中、`populate_initial_units` 在数据格上落子（牌面 / 四维 / `origin="initial"` / `owner_slot_id`）、队伍注入覆盖全盘含已落子单位、瞬时 `move_card` 源清空 + 目标全字段 + 保留 `owner_slot_id`/`origin` + **跨盘保留 `team_id`**、跨盘 `resolve_attack` 结算与阵亡名单。这条"无视图装配"路径此前**根本不存在**（只有 `create_main` 一条纯客户端路径）；已接入 CI。九套 headless 全绿，两条黄金路径哈希逐位不变（`652a3ef2…` / `0576fcc9…`）；分层棘轮仍为 87（中途一度因 `Callable.call` 引入 1 处 `reflection-call`，被 CI 拦下后改为 `use_view_cells` 分支写法） |

| **无头完整攻击结算：表现开关**（§3.4-6） | `CombatSystem` 新增 `presentation_enabled`（默认 `true`）：置 `false` 时 `attack_cells()` 只做"纯结算 + 死亡清算（入墓 / 除外 / on_kill）"，**不碰任何视图、不建 tween、不等待**；`move_card()` 也据此直接走瞬时数据转移（即使 `Game.instant_battle` 为 false）。该开关与 `instant_battle` **正交**（后者只压缩客户端"跳过动画"的等待），客户端默认值下 `attack_cells` 的代码路径与改动前逐字相同。调查还确认：`PlayController.handle_unit_death` / `handle_kills` **本身已是纯规则**（读 `cell.*` 鸭子类型、`Game.get_card`、`Effects`、`slot.graveyard`），UI 只存在于出牌动画路径 —— 所以无头侧只需一个"未 setup 的 PlayController"即可复用同一条清算逻辑 | 新增 `tests/HeadlessCombatTest`（**13 条断言**，真实卡库）：`CellData` 盘面上一次完整攻击（无表现模式）→ 结算生效 / 阵亡清空 / 攻击方存活 / **CombatSystem 未挂任何表现节点** / 阵亡已入墓；入墓路由两条（`origin="initial"` → 原属盘墓地；`origin="hand"` 且无归属牌库 → 兜底 `Game.deck.graveyard`）且**只入一处**；`instant_battle=false` 时 `move_card` 仍走瞬时路径且 `owner_slot_id`/`origin`/`team_id` 全保留。已接入 CI。十套 headless 全绿，两条黄金路径哈希逐位不变（`652a3ef2…` / `0576fcc9…`）；分层棘轮仍为 87 |

| **权威端棋盘结算：服务器裁决落子**（§4.1，反作弊闭环的第一步） | 新增 `scripts/server/authority_board.gd`（`AuthorityBoard`）：把棋盘规则引擎接进权威端的**适配器** —— `BattleAuthority` 保持纯状态机（RefCounted / 不碰场景树 / 无 SceneTree 可测），棋盘依赖全部封装在适配器里，两者只通过 `deploy_unit()` / `state()` 交互。建盘走 `BoardSlotFactory.create_headless`（同一套规则 + `CellData` 纯数据，不是第二份规则）；落子按卡库的 `CardUnit` 属性写入，并带上 slot 的 `team_id` / `owner_player_id`。`BattleAuthority.attach_board()` 接入后：`intent/play_card` 先由适配器校验并落子（**失败不消耗手牌与费用**，原子性），再扣费用与手牌并下发 `card_played` + `unit_deployed`；`view_for()` 新增 `board`（盘面公开，手牌仍只暴露数量）。未接入适配器时行为与原来完全一致（旧测试不受影响） | 新增 `tests/AuthorityBoardTest`（**25 条断言**，真实卡库）：建盘（两盘各 9 个 `CellData`、归属/队伍注入）、开局手牌与费用、**四类拒绝且全部零消耗**（打到对手盘 → `illegal_target`、缺落点 → `bad_payload`、越界 → `illegal_target`、非单位卡 → `not_allowed`）、合法落子（手牌 −1 / 费用 −1 / 盘面格子确有该单位且 `owner_slot_id`+`team_id`+`origin` 正确）、`auth/state` 带盘面且**对手可见单位但看不到手牌内容**、事件流 `card_played`+`unit_deployed`（带落点）、费用不足拒绝且不落子。已接入 CI。十一套 headless 全绿，两条黄金路径哈希逐位不变（`652a3ef2…` / `0576fcc9…`）；分层棘轮仍为 87 |

**阶段 3 剩余**：规则层读写改为直接面向 `CellData`（`cell.gd` 已退化为视图，393 → 346 行；剩余是让规则层直接面向 `CellData` 读状态、并把 `board_slot_factory` 的 `grid_cells` 类型从"节点或数据"收敛为纯数据）、`turn_system` 拉直 `await`（9 处 `create_timer` + 10 处 `get_tree`；无头跑完整回合还差"表现开关 + 死亡清算从 `PlayController` 抽出"这条缝）、`play_controller` 的 `Control` 依赖、**反作弊收口**（客户端接入权威路径：`action/*` → `intent/*`、`auth/state` 渲染、棋盘结算移入 `BattleAuthority`）、`main.gd` ⟷ `test_main.gd` 合一。

---

*本文档为评审稿；阶段 0 已完成，阶段 1 的 GDScript 侧与 Go 中继侧（止血四策略）均已完成，阶段 2 完成 10 个切片，阶段 3 已落地 定序/随机源/结算分离/清理/中继止血/结算去网络依赖/CellData 地基/Cell 视图化/无视图装配/无头战斗结算/权威端棋盘结算 11 项。*
